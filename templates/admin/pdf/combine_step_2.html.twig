{% extends 'form_base.html.twig' %}
{% import 'form_base.html.twig' as self %}

{% block content_title 'Combine pdf – Select responses'| trans %}

{% block form_header %}
    {{ self.value_row('Archiver', archiver.name, {change: {url: path('pdf_combine_step_0')}}) }}
    {{ self.value_row('Hearing', hearing.name, {change: {url: path('pdf_combine_step_1', {archiver: archiver.id})}}) }}

    {{ parent() }}
{% endblock %}
